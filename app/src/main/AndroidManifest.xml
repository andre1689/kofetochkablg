<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.kofetochka.kofetochkablg">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".LoginActivity">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER"/>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity>

        <activity android:name=".WorkActivity">

        </activity>

        <activity android:name=".OpenShift">

        </activity>

        <activity android:name=".CloseShiftActivity">

        </activity>

        <activity android:name=".NewOrderActivity">

        </activity>

        <activity android:name=".ApplicationPartActivity"></activity>
        <activity android:name=".NewCoffeeActivity"></activity>

    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>
